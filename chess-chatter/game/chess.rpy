image bg chess empty = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board empty")

image bg chess random = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board random")

image bg chess full = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board full")

image bg chess opening = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board opening")

image bg chess two figures = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board two figures")

image bg chess eaten = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board eaten")

image bg chess black attack = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board black attack")

image bg chess loosing queen = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board loosing queen")

image bg chess preparing counter = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board preparing counter")

image bg chess move knight = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board move knight")

image bg chess check rooks = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board check rooks")

image bg chess mate = LiveComposite(
    (1280, 640),
    (-70, 0), "bg chess",
    (-70, 0), "board mate")
